## Sentiment scores for documents

Install transformers in conda env:

conda create -n "neutrality" pip

pip install -q transformers

pip install torch

pip install nltk

pip install pandas

pip  install seaborn